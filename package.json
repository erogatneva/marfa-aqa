{
  "name": "marfa-aqa",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --browser chromium",
    "smoke": "npx playwright test ./tests/smoke.spec.ts --browser chromium",
    "init": "npm install && npx playwright install",
    "report": "npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.9"
  }
}
